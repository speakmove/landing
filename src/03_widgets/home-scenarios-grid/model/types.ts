/** Raw translation shape for a single scenario card (used only in the RSC data layer). */
export type TScenarioCard = {
  id: string;
  title: string;
  aiRole: string;
  hook: string;
  duration: string;
};
